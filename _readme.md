# nan

This directory contains a working copy curated from the external tree at ~/NaN. The original tree is left unchanged and treated as archival.

Active editing and reorganization happen here so changes can be reviewed without altering the source tree.
